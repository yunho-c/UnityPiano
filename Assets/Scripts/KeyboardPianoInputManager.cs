using UnityEngine;

// NOTE: Currently unused. Consider moving input-related logic into this class
//       for a cleaner separation of concerns (input/state mgmt vs. audio calls)

public class PianoKeyboardInputManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}